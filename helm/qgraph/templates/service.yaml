apiVersion: v1
kind: Service
metadata:
  name: {{ include "qgraph.fullname" . }}
  labels:
    {{- include "qgraph.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "qgraph.selectorLabels" . | nindent 4 }}
