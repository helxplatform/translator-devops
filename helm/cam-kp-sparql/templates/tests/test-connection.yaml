apiVersion: v1
kind: Pod
metadata:
  name: '{{ include "cam-kp-sparql.fullname" . }}-test-connection'
  labels:
    {{- include "cam-kp-sparql.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cam-kp-sparql.fullname" . }}:{{ .Values.camKpSPARQL.service.port }}']
  restartPolicy: Never
