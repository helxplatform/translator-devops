apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "icees-api.fullname" . }}-test-connection"
  labels:
    {{- include "icees-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "icees-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
