apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "answer-coalesce-test.fullname" . }}-test-connection"
  labels:
    {{- include "answer-coalesce-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "answer-coalesce-test.fullname" . }}-web-service:{{ .Values.ac.service.port }}/apidocs']
  restartPolicy: Never
