apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "strider.fullname" . }}-logs-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: {{ .Values.strider.logsPVCSize }}