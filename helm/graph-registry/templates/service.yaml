apiVersion: v1
kind: Service
metadata:
  name: {{ include "graph-registry.fullname" . }}
  labels:
    {{- include "graph-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "graph-registry.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
