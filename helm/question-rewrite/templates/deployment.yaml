apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "question-rewrite.fullname" . }}
  labels:
    {{- include "question-rewrite.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "question-rewrite.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "question-rewrite.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          ports:
            - name: http
              containerPort: {{ .Values.containerPort }}
              protocol: TCP
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
