
id-id:
  auth:
    enabled: true
  master:
    resources:
      requests:
        memory: #
    readinessProbe:
      enabled: false
    livenessProbe:
      enabled: false
    extraFlags:
    - "--save 60 10000"
    - "--appendonly no"
    - "--stop-writes-on-bgsave-error no"

  cluster:
    slaveCount: 0

id-categories:
  auth:
    enabled: true
  master:
    readinessProbe:
      enabled: false
    livenessProbe:
      enabled: false
    resources:
      requests:
        memory: #
    extraFlags:
    - "--save 60 10000"
    - "--appendonly no"
    - "--stop-writes-on-bgsave-error no"

  cluster:
    slaveCount: 0

id-eq-id:
  auth:
    enabled: true
  master:
    readinessProbe:
      enabled: false
    livenessProbe:
      enabled: false
    resources:
      requests:
        memory: #200Gi
    extraFlags:
    - "--save 60 10000"
    - "--appendonly no"
    - "--stop-writes-on-bgsave-error no"
  cluster:
    slaveCount: 0

semantic-count:
  auth:
    enabled: true
  master:
    readinessProbe:
      enabled: false
    livenessProbe:
      enabled: false
    resources:
      requests:
        memory: #
    extraFlags:
    - "--save 60 10000"
    - "--appendonly no"
    - "--stop-writes-on-bgsave-error no"
  cluster:
    slaveCount: 0

conflation-db:
  auth:
    enabled: true
  master:
    readinessProbe:
      enabled: false
    livenessProbe:
      enabled: false
    resources:
      requests:
        memory: #
    extraFlags:
    - "--save 60 10000"
    - "--appendonly no"
    - "--stop-writes-on-bgsave-error no"
  cluster:
    slaveCount: 0

info-content:
  auth:
    enabled: true
  master:
    readinessProbe:
      enabled: false
    livenessProbe:
      enabled: false
    resources:
      requests:
        memory: #
    extraFlags:
    - "--save 60 10000"
    - "--appendonly no"
    - "--stop-writes-on-bgsave-error no"
  cluster:
    slaveCount: 0
