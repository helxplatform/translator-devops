apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "yobokop.fullname" . }}-test-connection"
  labels:
    {{- include "yobokop.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "yobokop.fullname" . }}:{{ .Values.yobokop.service.port }}']
  restartPolicy: Never
