apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "shepherd.fullname" . }}-aragorn
  labels:
    {{- include "shepherd.labels" . | nindent 4 }}
    service-type: worker
spec:
  replicas: {{ .Values.aragorn.replicaCount }}
  {{ if .Values.aragorn.strategy }}
  strategy:
    type: {{ .Values.aragorn.strategy }}
  {{ end }}
  selector:
    matchLabels:
      {{- include "shepherd.selectorLabels" . | nindent 6 }}
      service-type: worker
  template:
    metadata:
      labels:
        {{- include "shepherd.selectorLabels" . | nindent 8 }}
        service-type: worker
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/configmap.yaml") .  | sha256sum }}
    spec:
      {{- with .Values.securityContext }}
      securityContext:
        {{ . | toYaml | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}-aragorn
          image: "{{ .Values.aragorn.image.repository }}:{{ .Values.shepherd.tag }}"
          imagePullPolicy: "{{ .Values.aragorn.image.imagePullPolicy }}"
          envFrom:
            - configMapRef:
                name: {{ include "shepherd.fullname" . }}-configmap
          command:
            - 'python'
          args:
            - "worker.py"
          {{ if .Values.aragorn.resources }}
          resources: {{ toYaml .Values.aragorn.resources | nindent 12 }}
          {{ end }}
          volumeMounts:
          - name: {{ include "shepherd.fullname" . }}-logs-pvc
            mountPath: /app/logs/
      volumes:
      - name: {{ include "shepherd.fullname" . }}-logs-pvc
        persistentVolumeClaim:
          claimName: {{ include "shepherd.fullname" . }}-logs-pvc
      {{- with .Values.shepherd.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.shepherd.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.shepherd.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
