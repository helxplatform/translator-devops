{{- if .Values.embeddingsLoader.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "shepherd.fullname" . }}-embeddings-loader
  labels:
    {{- include "shepherd.labels" . | nindent 4 }}
spec:
  backoffLimit: {{ .Values.embeddingsLoader.backoffLimit | default 1 }}
  ttlSecondsAfterFinished: {{ .Values.embeddingsLoader.ttlSecondsAfterFinished | default 300 }}
  template:
    metadata:
      labels:
        {{- include "shepherd.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: embeddings-loader
    spec:
      restartPolicy: Never
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: embeddings-loader
          image: "{{ .Values.embeddingsLoader.image.repository }}:{{ .Values.embeddingsLoader.image.tag }}"
          imagePullPolicy: {{ .Values.embeddingsLoader.image.pullPolicy }}
          command:
            - sh
            - -c
            - |
              set -e

              DEST="/data"
              SENTINEL="${DEST}/.data-ready"
              CHECKSUM_FILE="${DEST}/.source-url-hash"

              # Hash the source URLs to detect changes
              CURRENT_HASH=$(echo '{{ join "," .Values.embeddingsLoader.sources | sha256sum }}' | cut -c1-12)

              # Skip download if data exists and source hasn't changed
              {{- if .Values.embeddingsLoader.skipIfExists }}
              if [ -f "$SENTINEL" ] && [ -f "$CHECKSUM_FILE" ]; then
                EXISTING_HASH=$(cat "$CHECKSUM_FILE")
                if [ "$CURRENT_HASH" = "$EXISTING_HASH" ]; then
                  echo "Data already present and sources unchanged — skipping download."
                  exit 0
                else
                  echo "Source URLs changed — removing old files and re-downloading."
                  rm -rf ${DEST}/*
                fi
              fi
              {{- end }}

              echo "Downloading data files..."
              {{- range .Values.embeddingsLoader.sources }}

              echo "Fetching: {{ . }}"
              FILENAME=$(basename "{{ . }}" | sed 's/?.*//')

              {{- if or (hasSuffix ".tar.gz" .) (hasSuffix ".tgz" .) }}
              wget -q -O "${DEST}/${FILENAME}" "{{ . }}"
              tar xzf "${DEST}/${FILENAME}" -C "$DEST"
              rm -f "${DEST}/${FILENAME}"
              {{- else if hasSuffix ".tar" . }}
              wget -q -O - "{{ . }}" | tar xf - -C "$DEST"
              {{- else if hasSuffix ".zip" . }}
              wget -q -O "/tmp/${FILENAME}" "{{ . }}"
              unzip -o "/tmp/${FILENAME}" -d "$DEST"
              rm -f "/tmp/${FILENAME}"
              {{- else }}
              wget -q -O "${DEST}/${FILENAME}" "{{ . }}"
              {{- end }}
              {{- end }}

              {{- if .Values.embeddingsLoader.postDownloadCommand }}
              echo "Running post-download command..."
              {{ .Values.embeddingsLoader.postDownloadCommand }}
              {{- end }}

              # Mark download complete
              echo "$CURRENT_HASH" > "$CHECKSUM_FILE"
              date -u '+%Y-%m-%dT%H:%M:%SZ' > "$SENTINEL"
              touch "${DEST}/.loaded"
              echo "Data download complete."
          volumeMounts:
            - name: embeddings-data
              mountPath: {{ .Values.embeddingsLoader.sharedData.mountPath }}
          {{- with .Values.embeddingsLoader.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: embeddings-data
          persistentVolumeClaim:
            claimName: {{ include "shepherd.fullname" . }}-embeddings-data
      {{- with .Values.shepherd.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.shepherd.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}