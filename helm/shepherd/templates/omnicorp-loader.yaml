{{- if .Values.omnicorpLoader.enabled }}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "shepherd.fullname" . }}-omnicorp-loader
  labels:
    {{- include "shepherd.labels" . | nindent 4 }}
spec:
  backoffLimit: {{ .Values.omnicorpLoader.backoffLimit | default 1 }}
  ttlSecondsAfterFinished: {{ .Values.omnicorpLoader.ttlSecondsAfterFinished | default 300 }}
  template:
    metadata:
      labels:
        {{- include "shepherd.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: omnicorp-loader
    spec:
      restartPolicy: Never
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: omnicorp-loader
          image: "{{ .Values.omnicorpLoader.image.repository }}:{{ .Values.omnicorpLoader.image.tag }}"
          imagePullPolicy: {{ .Values.omnicorpLoader.image.pullPolicy }}
          command:
            - sh
            - -c
            - |
              set -e

              DEST="/data"
              SENTINEL="${DEST}/.data-ready"
              CHECKSUM_FILE="${DEST}/.source-url-hash"

              # Hash the source URLs to detect changes
              CURRENT_HASH=$(echo '{{ join "," .Values.omnicorpLoader.sources | sha256sum }}' | cut -c1-12)

              # Skip download if data exists and source hasn't changed
              {{- if .Values.omnicorpLoader.skipIfExists }}
              if [ -f "$SENTINEL" ] && [ -f "$CHECKSUM_FILE" ]; then
                EXISTING_HASH=$(cat "$CHECKSUM_FILE")
                if [ "$CURRENT_HASH" = "$EXISTING_HASH" ]; then
                  echo "Data already present and sources unchanged — skipping download."
                  exit 0
                else
                  echo "Source URLs changed — removing old files and re-downloading."
                  rm -rf ${DEST}/*
                fi
              fi
              {{- end }}

              echo "Downloading data files..."
              {{- range .Values.omnicorpLoader.sources }}

              echo "Fetching: {{ . }}"
              FILENAME=$(basename "{{ . }}" | sed 's/?.*//')

              {{- if or (hasSuffix ".tar.gz" .) (hasSuffix ".tgz" .) }}
              wget -q -O "${DEST}/${FILENAME}" "{{ . }}"
              tar xzf "${DEST}/${FILENAME}" -C "$DEST"
              rm -f "${DEST}/${FILENAME}"
              {{- else if hasSuffix ".tar" . }}
              wget -q -O - "{{ . }}" | tar xf - -C "$DEST"
              {{- else if hasSuffix ".zip" . }}
              wget -q -O "/tmp/${FILENAME}" "{{ . }}"
              unzip -o "/tmp/${FILENAME}" -d "$DEST"
              rm -f "/tmp/${FILENAME}"
              {{- else }}
              wget -q -O "${DEST}/${FILENAME}" "{{ . }}"
              {{- end }}
              {{- end }}

              {{- if .Values.omnicorpLoader.postDownloadCommand }}
              echo "Running post-download command..."
              {{ .Values.omnicorpLoader.postDownloadCommand }}
              {{- end }}

              # Mark download complete
              echo "$CURRENT_HASH" > "$CHECKSUM_FILE"
              date -u '+%Y-%m-%dT%H:%M:%SZ' > "$SENTINEL"
              touch "${DEST}/.loaded"
              echo "Data download complete."
          volumeMounts:
            - name: omnicorp-data
              mountPath: {{ .Values.omnicorpLoader.sharedData.mountPath }}
          {{- with .Values.omnicorpLoader.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      volumes:
        - name: omnicorp-data
          persistentVolumeClaim:
            claimName: {{ include "shepherd.fullname" . }}-omnicorp-data
      {{- with .Values.shepherd.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.shepherd.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}