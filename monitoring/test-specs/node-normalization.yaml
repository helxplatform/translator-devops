config:
  target: "{{ $processEnvironment.SERVER_URL }}"
  ensure:
    maxErrorRate: 1
    p95: 2
  phases:
    - duration: 1
      arrivalRate: 1
  plugins:
    expect: {}
scenarios:
  - flow:
      - get:
          url: "/docs"
          expect:
            - statusCode: 200
